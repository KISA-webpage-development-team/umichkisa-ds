# Phase 0.5 — Shared Layout (Notes)

Strict append-only. Breadcrumbs for decisions, blockers, and DS fixes discovered during execution.

## Autonomous routine — 2026-04-17

- Lane 0.5.1 (#47, client PR #57): bailed out with `needs-decision` — template.tsx currently renders Header+session, conflicts with plan's `(main)/layout.tsx` hosting Header/Footer. WIP commit contains only the unambiguous directory moves (74 renames).
- Lane 0.5.2 (#1, DS): Instagram icons added — `instagram` (mono, currentColor) + `instagram-brand` (official gradient, React.useId for gradient IDs). Registry updated, `ds-fixes-log.md` appended, `pnpm --filter @umichkisa-ds/web typecheck && build` green.
- Lane 0.5.2.bonus (#3, DS): exported `iconNames` from DS web package; docs `/components/icon` now iterates the registry via a client subcomponent (`IconRegistryGrid`) — server-side iteration fails because the DS bundle ships with a `"use client"` banner, which turns every named export into a Next.js client reference when imported into a server component. Surfaced during PR #2 review.
- Protocol amendment 2026-04-17: added post-merge label cleanup rule (§2, §8) — `ready-for-review` / `needs-revision` / `needs-decision` / `routine-errored` must be stripped at merge time. Surfaced because PR #2 was merged with a stale `ready-for-review` label still attached.

## Autonomous routine — 2026-04-18

- Lane 0.5.4a (#49, client PR #58): clean completion, `ready-for-review`. Moved `navbar-menu.jsx` out of `ui/aceternity/` into `layout/header/NavMenu.tsx` with minimal `.jsx → .tsx` type-only casts (typed `transition` as `Transition`, `menuVariants` as `Variants`, one inline `variants` cast, `HoveredLink` rest typed `any` to accept pass-through `href`); no class/animation/behavior changes. Created `HeaderTitle.tsx` merging `HeaderTitleBlock` + `WebLogo`. Deleted 9 dead files (Menu, MobileMenu, MobileMenuList, MobileMenuCloseButton, WebTitle, HeaderTitleBlock, WebLogo, header.module.css, features/jobs-curator/layout/HeaderTitle). Header.jsx imports repointed; `isJobsCurator` state + `JobsCuratorHeaderTitle` import dropped. `npx tsc --noEmit` + `npm run lint` green. `npm run build` fails on `/api/create-payment-intent` page-data due to missing Stripe key in autonomous VM env — reproduces on untouched `origin/dev`, flagged in PR body, not a regression.
- Lane 0.5.5 (#54, client PR #59 — draft): partial WIP, `needs-decision`. Renamed `Footer.jsx → Footer.tsx`, dropped `sejongHospitalLight/Bold` overrides, replaced the `UMICH KISA` wordmark home link with a `type-caption` copyright line, and swapped `FacebookLinkIcon` → DS `<Icon name="mail" size="md" />` inside a `<a href="mailto:umichkisa@gmail.com" rel="nofollow noreferrer">`. Bailed out on the Instagram swap per the issue's explicit trigger — `instagram-brand` isn't in installed DS `1.0.0` (registry has it on DS `main`, but no version bump has shipped; client still pins `1.0.0`). Draft PR asks which coordination option to take (early DS bump vs. merge-as-is with trailing commit vs. hold). Left `InstagramLinkIcon` in place with a `TODO(lane-0.5.5)` pointer for the follow-up.
