# Phase 0.5 — Shared Layout (Notes)

Strict append-only. Breadcrumbs for decisions, blockers, and DS fixes discovered during execution.

## Autonomous routine — 2026-04-17

- Lane 0.5.1 (#47, client PR #57): bailed out with `needs-decision` — template.tsx currently renders Header+session, conflicts with plan's `(main)/layout.tsx` hosting Header/Footer. WIP commit contains only the unambiguous directory moves (74 renames).
- Lane 0.5.2 (#1, DS): Instagram icons added — `instagram` (mono, currentColor) + `instagram-brand` (official gradient, React.useId for gradient IDs). Registry updated, `ds-fixes-log.md` appended, `pnpm --filter @umichkisa-ds/web typecheck && build` green.
- Lane 0.5.2.bonus (#3, DS): exported `iconNames` from DS web package; docs `/components/icon` now iterates the registry via a client subcomponent (`IconRegistryGrid`) — server-side iteration fails because the DS bundle ships with a `"use client"` banner, which turns every named export into a Next.js client reference when imported into a server component. Surfaced during PR #2 review.
- Protocol amendment 2026-04-17: added post-merge label cleanup rule (§2, §8) — `ready-for-review` / `needs-revision` / `needs-decision` / `routine-errored` must be stripped at merge time. Surfaced because PR #2 was merged with a stale `ready-for-review` label still attached.

## Autonomous routine — 2026-04-18

- Lane 0.5.4a (#49, client PR #58): clean completion, `ready-for-review`. Moved `navbar-menu.jsx` out of `ui/aceternity/` into `layout/header/NavMenu.tsx` with minimal `.jsx → .tsx` type-only casts (typed `transition` as `Transition`, `menuVariants` as `Variants`, one inline `variants` cast, `HoveredLink` rest typed `any` to accept pass-through `href`); no class/animation/behavior changes. Created `HeaderTitle.tsx` merging `HeaderTitleBlock` + `WebLogo`. Deleted 9 dead files (Menu, MobileMenu, MobileMenuList, MobileMenuCloseButton, WebTitle, HeaderTitleBlock, WebLogo, header.module.css, features/jobs-curator/layout/HeaderTitle). Header.jsx imports repointed; `isJobsCurator` state + `JobsCuratorHeaderTitle` import dropped. `npx tsc --noEmit` + `npm run lint` green. `npm run build` fails on `/api/create-payment-intent` page-data due to missing Stripe key in autonomous VM env — reproduces on untouched `origin/dev`, flagged in PR body, not a regression.
- Lane 0.5.5 (#54, client PR #59 — draft): partial WIP, `needs-decision`. Renamed `Footer.jsx → Footer.tsx`, dropped `sejongHospitalLight/Bold` overrides, replaced the `UMICH KISA` wordmark home link with a `type-caption` copyright line, and swapped `FacebookLinkIcon` → DS `<Icon name="mail" size="md" />` inside a `<a href="mailto:umichkisa@gmail.com" rel="nofollow noreferrer">`. Bailed out on the Instagram swap per the issue's explicit trigger — `instagram-brand` isn't in installed DS `1.0.0` (registry has it on DS `main`, but no version bump has shipped; client still pins `1.0.0`). Draft PR asks which coordination option to take (early DS bump vs. merge-as-is with trailing commit vs. hold). Left `InstagramLinkIcon` in place with a `TODO(lane-0.5.5)` pointer for the follow-up.

## Autonomous routine — 2026-04-18 (evening pass)

- Lane 0.5.4b (#50, client PR #61): clean completion, `ready-for-review`. Retokenized `NavMenu.tsx` — all `michigan-blue` / `michigan-maize` → DS brand tokens (`brand-primary`, `brand-accent`); text classes → `type-body-sm md:type-body` paired with `text-white`; dropped `shadow-inner`; dropped `framer-motion` in full. Desktop submenu entrance swapped to `animate-in fade-in-0 zoom-in-95 duration-200`; mobile accordion + mobile nav wrapper swapped to `overflow-hidden transition-all duration-300` with `max-h-0 ↔ max-h-96 / max-h-screen`. Incidental cleanups in scope: `opacity-[0.9]` → `opacity-90`, `pt-[calc(2.4rem)]` → `pt-10`, `max-h-[1000px/2000px]` → built-in scale. Tightened prop types per component. `ds-client-review` PASS after one round (first pass flagged missing base color token paired with `type-*`; fixed by adding `text-white` on-element). `npx tsc --noEmit` + `npm run lint` green; `npm run build` skipped (same Stripe env-var environmental fail as prior phase-0.5 PRs).
- Lane 0.5.4d (#51, client PR #62 — draft): bailout, `needs-decision`. Two compounding blockers: (A) Vitest 4 + rolldown can't parse JSX in `.tsx` test files without `@vitejs/plugin-react`, which §9 forbids installing autonomously — first `.tsx` test in repo, Phase 0 infra gap. (B) New prop API `{ isAuthenticated, size? }` breaks 3 consumers typecheck-wise (`(pocha)/pocha/history/page.tsx`, `ui/feedback/NotLogin.tsx`, `features/pocha/components/manage/PochaManagePageHeader.tsx`) — all pass `session={...}` or `handleGoogleSignIn={...}` to the old API; sweeping them is scope drift per §6. Committed: LoginButton.jsx → .tsx with DS `Button variant="secondary"`, `useMockAuth()` wired, `React.memo`, spec-clean tests (3 runnable + 2 `.skip`'d real-mode) that can't yet parse. PR body has option matrix for both blockers.
- Lane 0.5.4f (#53, client PR #63): clean completion, `ready-for-review`. `MobileMenuButton.jsx → .tsx`, dropped `framer-motion` (motion.button + iconVariants), swapped deprecated `BurgerMenuIcon`/`CancelIcon` for DS `<Icon name="menu"/"x" size="md">`. Animation: two stacked absolute-positioned icon `<span>`s cross-fading via `transition-opacity duration-300` on `isMobileMenuOpen` — chose cross-fade over single-icon rotate for cleaner state semantics. Added `aria-label` + `aria-expanded` on the button. `ds-client-review` PASS first round (no violations). `npx tsc --noEmit` green; `npm run build` skipped (same env reason).
- Not eligible this run: 0.5.4c (needs-interactive — REDESIGN + auth hook consumption), 0.5.4e (needs-interactive — REDESIGN), 0.5.6 (needs-interactive — publish step).

## Decision amendment — 2026-04-18 (audit locked decision #9)

Audit locked decision #9 was "Drop framer-motion from header layer entirely; CSS-only animations". Revised during PR #61 review to **"Drop framer where it costs nothing; keep framer where it provides real visual value"**.

Rationale: `framer-motion ^12.23.6` is still imported by `src/features/home-sponsor/HomeCarousel.jsx` and `src/features/home-sponsor/SchoolCalendar.jsx`, and Phase 0.5 isn't touching those. So framer is installed in the bundle regardless of what the header layer does — the "drop framer" rule in the header bought zero bundle-size savings, zero maintenance savings, and cost the polish of the original spring entrances on the desktop submenu.

Applied scope of the amendment:
- **Desktop submenu entrance** (`MenuItem` in `NavMenu.tsx`) — **keep framer spring** (mass 0.5, damping 11.5, stiffness 100) + `layoutId="active"` for hover-transitions between items. Matches original exactly. Retokenization to DS brand-primary/brand-accent still applies.
- **Mobile accordion** (`MobileMenuItem`) — **stay CSS-only**. Simple height/opacity transitions didn't benefit from spring physics; original used framer only because the rest of the file did.
- **Mobile nav wrapper** (`MobileMenu` + `Menu`) — **stay CSS-only**. Same reason.
- **MobileMenuButton** (lane 0.5.4f, PR #63) — **stay CSS-only** (cross-fade of stacked icons). Two-state toggle doesn't need a physics engine; framer there was dead weight.
- **Header.jsx mobile right-side fade-in** (lane 0.5.4c, pending) — reassess in that lane. Likely stay CSS-only (simple opacity transition).

Policy going forward: when considering dropping a runtime lib from a file, first check whether the lib is imported elsewhere in the app. If yes, the drop doesn't save anything, so only drop when CSS-only is strictly better (simpler, more consistent with peer components). When framer provides a visible polish win (springs, layoutId, shared-layout transitions), keep it — the cost is already paid.

Surfaced by PR #61 review comments on 2026-04-18 (maintainer: "dropping framer-motion is cool? I don't see any benefits here"). Applied in client PR #61 commit 8471249.

## Autonomous routine — 2026-04-18 (late pass)

- Lane 0.5.4c (#55, client PR #66 — draft): partial WIP, `needs-decision`. Did every unambiguous locked decision: `Header.jsx → Header.tsx` rewrite, `Header()` drops `session` prop and consumes `useMockAuth()` with null-safe `?./??` field access (email/image/name), explicit state typing, `usePathname`/`useEffect` preserved; `app/(main)/layout.tsx` gradient retokenized `michigan-blue → brand-primary`, `sejongHospitalLight` import + className dropped, `<Header session=...>` → `<Header />`; `InstagramLinkIcon` → DS `<Icon name="instagram" size="md">` inside a `target=_blank` Link, `VerticalDivider` → DS `<Divider orientation="vertical" className="hidden lg:block h-8 border-white/40">`; commented-out logo block removed, breakpoint note collapsed, `ml-*` wrappers dropped; Latin-text `text-sm` wrappers swapped to `type-body-sm text-white`. Bailed out on the mobile right-side fade-in acceptance — issue's locked decision + acceptance both specify `animate-in fade-in-0 duration-300` *and* flag `tw-animate-css` as a DS prerequisite; that prerequisite has not landed (no import in `packages/web/theme.css`, no dep in DS `package.json`, no `ds-fixes-log.md` entry). Drafted the mobile chip with a plain `transition-opacity duration-300` + always-mount + `pointer-events-none` gate, matching the 2026-04-18 decision #9 amendment, and surfaced a 3-option matrix in the PR body (accept the CSS-transition deviation / land the DS prerequisite first / restore framer for this one fade per the amendment). `npx tsc --noEmit` + `npm run lint` green; `npm run build` skipped (same Stripe env-var environmental fail as prior phase-0.5 PRs); `ds-client-review` not run in this bailout pass — flagged `border-white/40` on the Divider is the likely reviewer note (mirrors the approved white-on-brand-primary exception from NavMenu).
- Not eligible this run: 0.5.4e (merged earlier today), 0.5.6 (needs-interactive — publish step).

## Interactive resolution — 2026-04-19

- Lane 0.5.4c (#55, client PR #66 — merged via squash `9cae858`): closed the needs-decision bailout. Grilled the 3-option matrix; **shipped Option 1** (CSS `transition-opacity duration-300` on the always-mounted `md:hidden` chip). Rejected Options 2 + 3: `tw-animate-css` is out of scope (natural owner = first Dialog/Popover/Sheet lane, not a one-off for this chip); framer rejected because Header is app-wide and the "Zero `framer-motion` imports in `Header.tsx`" acceptance bullet stands (amendment #9 softened the global rule, not this bullet). Spec reconciliation: issue #55 decision #5 + fade bullet resolved via a [comment on the issue](https://github.com/KISA-webpage-development-team/KISA-website-client/issues/55#issuecomment-4275192925), not by editing the locked decision log — overrides live as amendments + comments.
- `ds-client-review` round 1 surfaced 5 violations: V1/V2 (template-literal class concat → `cn()` from `@umichkisa-ds/web`), V4 (`gap-3 lg:gap-4` on the desktop right cluster → `gap-4`, Component tier), V5 (`md:gap-8` on the left cluster → `md:gap-6`, Section tier), and **V3 (manual page-shell composition)** — both `Header.tsx` `headerContentWidth` and `app/(main)/layout.tsx` `mainContentsWidth` violate `DS_CLIENT_USAGE.md#Layout` by composing `mx-auto max-w-screen-2xl px-4 md:px-24 lg:px-32` instead of using DS `Container`. V1/V2/V4/V5 fixed in commit `6bcf4fc`; round 2 PASS (0 violations, 1 trailing warning that falls with V3).
- **V3 deferred** to a dedicated site-wide follow-up lane — tracked at [client#67](https://github.com/KISA-webpage-development-team/KISA-website-client/issues/67). Deferral rationale: DS `Container` defaults to `md:px-6 lg:px-8` while the KISA brand layout uses `md:px-24 lg:px-32`; swapping mechanically would shrink horizontal padding by ~80–96px each side on md+. DS-side decision needed first — either ship a wider `Container` variant or widen the default. Timing: **Phase 0.5 → Phase 1 transition task**, not a Phase 1 blocker (Phase 1 lanes work inside `layout.tsx`'s shell rather than creating new ones) but should land before Phase 1 wraps to avoid compounding the violation across every subsequent ds-client-review.
- No DS code or tokens changed in this lane — docs only (this file).

## DS fix — 2026-04-19 (during lane 0.5.6 execution)

- `packages/web/src/components/layout/Container.tsx`: added `xl` size variant (`max-w-screen-xl`, 1280px), sits between `lg` (1024px) and `default` (1536px). Surfaced during lane 0.5.6 visual review — Container default felt too wide, `lg` felt too narrow for the KISA shell. DS build + typecheck green; client typecheck clean (pre-existing symlink-induced TS2742 warnings on unrelated shadcn badge/button files — not caused by this fix). Logged in `ds-fixes-log.md`. Commit `d9c671b`.
