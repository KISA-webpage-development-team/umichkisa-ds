# TODO

## Post-Migration

- [ ] Add consumer app example section to home page (`/`) — showcase the client app using DS components (after client app migration to `@umichkisa-ds/web`)

## Migrations

- [ ] Migrate `kisa-web` to `@umichkisa-ds/web` + `@umichkisa-ds/form`
- [ ] Migrate `pocha` app to `@umichkisa-ds/web` + `@umichkisa-ds/form`
