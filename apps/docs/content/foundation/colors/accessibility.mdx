# Accessibility

Color contrast is the ratio between the brightness of text and the brightness of
its background. A ratio of 1:1 means no contrast at all — white text on white.
A ratio of 21:1 is the maximum — black text on white.

WCAG AA is the industry standard accessibility level. It requires:
- **4.5:1** minimum for normal body text
- **3:1** minimum for large text (18px and above, or 14px bold)
- **3:1** minimum for UI components such as button borders and input outlines

The table below shows the contrast ratios for the token pairs you will use most
often. Pairs labeled **AA** pass WCAG AA for normal text. Pairs labeled
**Large only** pass only for large text (18px+ or 14px bold). Pairs labeled
**By design** intentionally fail — they are designed that way.

<ContrastTable rows={[
  { foreground: "--color-foreground",          background: "--color-surface",        ratio: "16.0:1", passes: "aa" },
  { foreground: "--color-muted-foreground",    background: "--color-surface",        ratio: "4.6:1",  passes: "aa" },
  { foreground: "--color-disabled-foreground", background: "--color-surface",        ratio: "2.8:1",  passes: "intentional-fail" },
  { foreground: "--color-link",                background: "--color-surface",        ratio: "5.9:1",  passes: "aa" },
  { foreground: "--color-brand-foreground",    background: "--color-brand-primary",  ratio: "8.2:1",  passes: "aa" },
  { foreground: "--color-brand-foreground",    background: "--color-brand-primary-hover",   ratio: "7.2:1",  passes: "aa" },
  { foreground: "--color-brand-foreground",    background: "--color-brand-primary-pressed", ratio: "10.3:1", passes: "aa" },
  { foreground: "--color-foreground",          background: "--color-surface-subtle", ratio: "14.5:1", passes: "aa" },
  { foreground: "--color-muted-foreground",    background: "--color-surface-subtle", ratio: "4.2:1",  passes: "large-only" },
  { foreground: "--color-muted-foreground",    background: "--color-surface-muted",  ratio: "3.8:1",  passes: "large-only" },
  { foreground: "--color-brand-accent",        background: "--color-brand-primary",  ratio: "8.2:1",  passes: "aa" },
  { foreground: "--color-foreground",          background: "--color-brand-accent",   ratio: "9.8:1",  passes: "aa" },
  { foreground: "--color-error",         background: "--color-surface",        ratio: "3.9:1",  passes: "large-only" },
  { foreground: "--color-success",       background: "--color-surface",        ratio: "2.2:1",  passes: "intentional-fail" },
  { foreground: "--color-warning",       background: "--color-surface",        ratio: "3.0:1",  passes: "large-only" },
  { foreground: "--color-info",          background: "--color-surface",        ratio: "5.9:1",  passes: "aa" },
]} />

<div className="mt-4 rounded-lg px-4 py-3 text-sm" style={{ background: "var(--color-surface-subtle)", border: "1px solid var(--color-border)" }}>
  <p className="font-semibold mb-1" style={{ color: "var(--color-foreground)" }}>Legend</p>
  <ul className="space-y-1" style={{ color: "var(--color-muted-foreground)" }}>
    <li><strong style={{ color: "oklch(35% 0.12 145)" }}>AA</strong> — Passes WCAG AA for all text sizes (4.5:1+).</li>
    <li><strong style={{ color: "oklch(48% 0.14 55)" }}>Large only</strong> — Passes WCAG AA for large text only (18px+ or 14px bold, 3:1+). Do not use at small sizes.</li>
    <li><strong style={{ color: "var(--color-muted-foreground)" }}>By design</strong> — Intentionally below contrast thresholds. See notes below for rationale.</li>
  </ul>
</div>

**On `--color-disabled-foreground`:** The 2.8:1 ratio is intentional. Disabled
elements are not meant to be read — they communicate "this action is not
available." If a user needs to read it, it should not be disabled.

**On `--color-muted-foreground` over `--color-surface-subtle`:** This combination
passes at 4.2:1 for large text only. Avoid using muted text at small sizes
inside cards — use `--color-foreground` instead if the content matters.

**On `--color-muted-foreground` over `--color-surface-muted`:** This combination (3.8:1)
passes for large text only. Surface-muted is used inside cards for detail rows —
avoid muted text at small sizes in these regions. Use `--color-foreground`
instead if the content needs to be readable.

**On `--color-brand-foreground` over `--color-brand-primary`:** This is the
strongest verified pair in the system at 8.2:1 — maize on navy. It clears
WCAG AA at every text size and weight. Whenever text sits on a brand-primary
background, this is the combination to reach for. It is also the combination
that defines KISA's visual identity.

**On `--color-brand-foreground` over hover and pressed states:** Maize remains
valid on `--color-brand-primary-hover` (7.2:1) and `--color-brand-primary-pressed`
(10.3:1). Both clear WCAG AA. There is no need for a different text token on
interactive states.

**On feedback colors over `--color-surface`:** `--color-error` (3.9:1) passes
for large text only — do not use it as small body text. `--color-warning`
(3.0:1) meets the non-text contrast threshold exactly — treat it as a floor,
not a comfortable pass, and always pair it with a `--color-foreground` label
for any readable content. `--color-success` (2.2:1) fails both text and
non-text contrast thresholds — never use it as standalone text or icon color;
always pair it with a `--color-foreground` label. `--color-info` (5.9:1)
passes for all text sizes.
