/**
 * compile-design.ts — KISA DS DESIGN.md compiler
 *
 * Reads `src/styles/index.css` + `src/tokens/primitives.css`, resolves the
 * token graph, and writes `/DESIGN.md` at the repo root. Spec: A4
 * (`docs/refactor/A4-design-compile.md`). Wired as `prebuild` in
 * `packages/web/package.json` so the artifact regenerates on every package
 * build.
 *
 * Stub state — implementation lands in C1.3.
 */

console.error('compile-design: not implemented yet (C1.2 scaffold)');
process.exit(1);
